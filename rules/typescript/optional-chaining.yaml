name: optional-chaining
description: Migrate manual null checks to optional chaining syntax
language: typescript
version: "1.0"

pattern:
  before: "$obj && $obj.$prop"
  after: "$obj?.$prop"

config:
  include:
    - "src/**/*.ts"
    - "src/**/*.tsx"
